#include<stdio.h>
int main()
{
	int i,n,a[n],x;
	printf("Enter number of elements for array\n");
	scanf("%d",&n);
	printf("Enter elements of array\n");
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	printf("Enter Target element\n");
	scanf("%d",&x);
	int occurrence=0,result;
	for(i=0;i<n;i++)
	{
		if(a[i]==x)
		{
			occurrence++;
		}
		
	}

	printf("%d is repeated %d times in the array",x,occurrence);

}
